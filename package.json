{
  "name": "saulo-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Portfólio Pessoal de Saulo Santos",
  "scripts": {
    "dev": "npx live-server --port=3000",
    "start": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
